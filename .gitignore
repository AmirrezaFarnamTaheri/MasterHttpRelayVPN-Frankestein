# Rust (any crate under the repo)
target/
dist/
/ca
/config.json
.env
.vscode/
.idea/
*.code-workspace
.DS_Store
Thumbs.db
/SCR-*.png
**/__pycache__/
*.pyc
# Android SDK path (machine-local); CI builds the release APK — local Gradle optional.
android/local.properties

# Local Gradle cache accidentally created in repo root (should never be committed).
.gradle-user-home/
