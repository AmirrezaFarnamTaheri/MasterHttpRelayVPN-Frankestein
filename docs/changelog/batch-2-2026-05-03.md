# Changelog — Batch 2: Desktop ConfigWire ↔ registry drift gate (2026-05-03)

## Summary

- **What changed:** Added **`tools/check-config-wire-vs-registry.py`**, requiring **`ConfigWire`** field names in **`src/bin/ui.rs`** to match top-level keys in **`docs/config-registry.json`** (same key set as serialized **`Config`**). Wired into **`tools/run-repo-sanity.py`** / CI **repo-sanity**.
- **Why:** Roadmap **BATCH-2** / **BATCH-3** already added Rust UI regression tests for Desktop save parity; this gate catches structural drift when **`Config`** gains a root field but **`ConfigWire`** forgets to mirror it—before runtime tests need updating.

## Details

### 1) Mechanical parity: registry roots vs `ConfigWire`

- **Files changed**: `tools/check-config-wire-vs-registry.py`, `tools/run-repo-sanity.py`, `tools/README.md`, `docs/release-checklist.md`.
- **Desktop impact**: none at runtime (metadata-only guard).
- **Tests run**:
  - `python tools/check-config-wire-vs-registry.py`
  - `cargo test config_wire --features ui --bin mhrv-f-ui`

## Verification

- [x] `python tools/check-config-wire-vs-registry.py`
- [x] `cargo test config_wire --features ui --bin mhrv-f-ui`
- [x] `python tools/run-repo-sanity.py --skip-readiness --skip-node` (includes new step)

## Relation to earlier roadmap batches

- **BATCH-2 / BATCH-3** in **`elevation_audit_roadmap_source.md`** delivered migration + **`ConfigWire`** field fixes + **`config_wire_serializes_all_current_config_fields_when_present`**. Batch 2 (this changelog) adds a **static** registry ↔ struct inventory check complementary to those tests.

## Remaining risk

- **`ConfigWire`** fields using **`serde(rename)`** would require extending the checker (currently assumes JSON keys match Rust field names).
