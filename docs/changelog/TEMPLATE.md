# Batch N - Short Title - YYYY-MM-DD

## Summary

Explain the user-visible or maintainer-visible outcome in one or two short
paragraphs.

## Changed

- Code, docs, config, backend helpers, UI, tests, tooling, or generated files
  changed in this batch.

## UI / UX

- Desktop impact.
- Android impact.
- CLI/support-bundle impact.
- Note `N/A` when there is intentionally no UI surface.

## Config / Schema

- Config keys, JSON contracts, readiness IDs, support snapshot schemas, status
  schemas, examples, or generated docs changed.
- Note `N/A` when config/schema did not change.

## Backend Helpers

- Apps Script, Cloudflare Worker, Vercel/Netlify, tunnel-node, or exit-node
  behavior changed.
- Note `N/A` when backend behavior did not change.

## Security / Trust

- Redaction, signing, CA trust, tokens, exposed listeners, support sharing, or
  release authority changed.
- Note `N/A` when security/trust did not change.

## Docs

- Public docs, Persian docs, tool docs, release notes, roadmap, or generated
  docs changed.

## Breaking / Cleanup

- Removed legacy/deprecated/stale behavior.
- State whether backward compatibility was intentionally not preserved.

## Parity

- Desktop / Android / CLI / backend / docs parity notes.
- Explain any intentionally platform-specific behavior.

## Race / Split-Brain Review

- State ownership boundaries and stale-result/concurrency protections.
- Note `N/A` only if no stateful/runtime path changed.

## Verification

- Commands run.
- Explicitly mention skipped checks and why.

## Cleanup

- Generated files refreshed.
- Temporary outputs/caches removed.
- Legacy/deprecated files removed or intentionally retained.
