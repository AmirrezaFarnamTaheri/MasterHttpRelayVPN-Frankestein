# Readiness Matrix

Generated by `tools/generate-readiness-contract.ps1` from `src/readiness.rs`. Do not edit by hand; update the Rust readiness catalog and regenerate.

| ID | Severity | Applies to | OK when | Not OK when | Repair target | Desktop anchor | Android anchor |
|---|---|---|---|---|---|---|---|
| `config.version` | Blocker | Config validation | config_version is supported by this binary | config_version is newer than the binary understands | `config.version` | Config file -> config_version compatibility | Imported config file -> config_version compatibility |
| `config.mode` | Blocker | All modes | mode is apps_script, vercel_edge, direct, or full | mode is missing, misspelled, or a removed legacy value | `setup.mode` | Setup -> Mode | Mode section -> Mode dropdown |
| `account_groups.enabled` | Blocker | apps_script, full | at least one account group is enabled | all account groups are disabled or absent | `setup.account_groups` | Advanced -> Multi-account pools -> Add group | Apps Script relay -> Deployment IDs |
| `account_groups.script_ids` | Blocker | apps_script, full | enabled account groups contain at least one deployment ID | enabled account groups have no deployment IDs | `setup.account_groups.script_ids` | Advanced -> Multi-account pools -> IDs | Apps Script relay -> Deployment IDs |
| `account_groups.auth_key` | Blocker | apps_script, full | every enabled account group has an AUTH_KEY | an enabled account group has an empty AUTH_KEY | `setup.account_groups.auth_key` | Advanced -> Multi-account pools -> Auth key | Apps Script relay -> AUTH_KEY |
| `vercel.base_url` | Blocker | vercel_edge | vercel.base_url is an http(s) origin with a host | base URL is empty, malformed, or includes the relay path | `setup.serverless.base_url` | Setup -> Serverless JSON relay -> Base URL | Serverless JSON relay -> Base URL |
| `vercel.relay_path` | Blocker | vercel_edge | vercel.relay_path starts with / | relay path is empty or missing the leading / | `setup.serverless.relay_path` | Setup -> Serverless JSON relay -> Relay path | Serverless JSON relay -> Relay path |
| `vercel.auth_key` | Blocker | vercel_edge | vercel.auth_key is non-empty and not a placeholder | AUTH_KEY is empty or still uses a known placeholder | `setup.serverless.auth_key` | Setup -> Serverless JSON relay -> Auth key | Serverless JSON relay -> AUTH_KEY |
| `vercel.max_body_bytes` | Blocker | vercel_edge | vercel.max_body_bytes is at least 1024 | max_body_bytes is too small for a valid relay response | `advanced.serverless.max_body_bytes` | Setup -> Serverless JSON relay -> Max body MB | Advanced config import -> vercel.max_body_bytes |
| `direct.google_ip` | Blocker | direct | google_ip is empty for auto-detect or parses as an IP | google_ip is non-empty and not an IP address | `setup.direct.google_ip` | Network -> Google IP | Network -> Google edge IP |
| `direct.front_domain` | Blocker | direct | front_domain is empty or a valid hostname | front_domain is an IP address or invalid SNI name | `setup.direct.front_domain` | Network -> Front domain | Network -> Front SNI |
| `local.listener` | Blocker | all modes | listen_host is non-empty and listen_port is valid | local HTTP listener host or port is unusable | `setup.local_listener` | Network -> Listen host / Ports | Advanced -> LAN sharing plus built-in local listener defaults |
| `local.ports` | Blocker | Config validation | HTTP and SOCKS5 listeners use different ports | listen_port and socks5_port collide | `setup.local_listener.ports` | Network -> Ports | Advanced config import -> listen_port / socks5_port |
| `ca.trust` | Warning | apps_script, vercel_edge, direct | generated local CA is installed and trusted by the client | HTTPS clients may reject locally generated MITM leaves | `setup.ca_trust` | Setup header -> Install CA or CLI --install-cert | Install MITM certificate button |
| `ca.android_app_trust` | Warning | Android apps_script, vercel_edge, direct | target Android app/browser trusts user-installed CAs | Android 7+ app ignores user CAs unless it opts in | `help.android.ca_trust` | Help & docs -> Android CA trust | Help -> Certificate section |
| `lan.exposure` | Warning | LAN-bound listeners | LAN exposure is intentional and firewall scope is understood | proxy is bound to 0.0.0.0 or :: unexpectedly | `network.lan.listen_host` | Network -> Sharing and per-app routing -> Local only / Share on LAN | Advanced -> LAN sharing |
| `lan.token` | Warning | LAN-bound HTTP/CONNECT | lan_token or lan_allowlist is configured | LAN clients can reach HTTP/CONNECT without an access guard | `network.lan.access_control` | Network -> Sharing and per-app routing -> LAN token / Allowed IPs | Advanced -> LAN sharing -> LAN token / Allowed IPs |
| `lan.allowlist` | Warning | LAN-bound SOCKS5 | lan_allowlist contains at least one entry | SOCKS5 is exposed on LAN without token-capable access control | `network.lan.allowlist` | Network -> Sharing and per-app routing -> Allowed IPs | Advanced -> LAN sharing -> Allowed IPs |
| `full.codefull_deployment` | Warning | full | each Apps Script deployment uses CodeFull.gs | deployment may still run classic Code.gs | `help.full.codefull` | Help & docs -> Full tunnel -> CodeFull.gs | Help -> Full tunnel setup |
| `full.tunnel_node_url` | Warning | full | CodeFull.gs TUNNEL_SERVER_URL points to the tunnel-node origin | CodeFull.gs points at the wrong origin or includes /tunnel | `help.full.tunnel_node_url` | Help & docs -> Full tunnel -> TUNNEL_SERVER_URL | Help -> Full tunnel setup -> tunnel-node URL |
| `full.tunnel_auth` | Warning | full | CodeFull.gs and tunnel-node share the same TUNNEL_AUTH_KEY | Apps Script and tunnel-node secrets differ | `help.full.tunnel_auth` | Help & docs -> Full tunnel -> TUNNEL_AUTH_KEY | Help -> Full tunnel setup -> matching auth |
| `full.udp_support` | Warning | full | socks5_port is configured when app-level UDP is required | UDP-capable clients have no SOCKS5 UDP ASSOCIATE path | `network.local_socks5` | Network -> Ports -> SOCKS5 | Advanced config import -> socks5_port |
| `full.tunnel_health` | Warning | full | tunnel-node health/details passes and egress IP smoke test matches | tunnel-node is unverified, version-skewed, or egress differs | `help.full.tunnel_health` | Help & docs -> Full tunnel -> health/details and IP-check smoke test | Help -> Full tunnel setup -> health and public-IP verification |
| `scan.batch_size` | Blocker | Config validation | scan_batch_size is at least 1 | scan batch size is zero | `advanced.scan.batch_size` | Advanced -> Scan settings -> scan_batch_size | Advanced config import -> scan_batch_size |
| `relay.rate_limit_qps` | Blocker | Config validation | relay_rate_limit_qps is unset or a positive finite number | relay_rate_limit_qps is zero, negative, NaN, or infinite | `advanced.relay_rate_limit_qps` | Advanced -> Relay tuning -> relay_rate_limit_qps | Advanced config import -> relay_rate_limit_qps |
| `domain_overrides.host` | Blocker | Config validation | domain override host or suffix is non-empty | domain override has no host/suffix match | `advanced.domain_overrides.host` | Advanced -> Domain overrides -> host/suffix | Advanced config import -> domain_overrides |
| `domain_overrides.force_route` | Blocker | Config validation | force_route is direct, sni_rewrite, relay, or full_tunnel | force_route uses an unknown route name | `advanced.domain_overrides.force_route` | Advanced -> Domain overrides -> force_route | Advanced config import -> domain_overrides |
| `fronting_groups.name` | Blocker | Config validation | fronting group name is non-empty | fronting group name is blank | `advanced.fronting_groups.name` | Advanced -> Fronting groups -> name | Advanced config import -> fronting_groups |
| `fronting_groups.ip` | Blocker | Config validation | fronting group IP parses as an IP address | fronting group IP is missing or malformed | `advanced.fronting_groups.ip` | Advanced -> Fronting groups -> IP | Advanced config import -> fronting_groups |
| `fronting_groups.sni` | Blocker | Config validation | fronting group SNI is a valid hostname | fronting group SNI is missing, an IP address, or invalid | `advanced.fronting_groups.sni` | Advanced -> Fronting groups -> SNI | Advanced config import -> fronting_groups |
| `fronting_groups.domains` | Blocker | Config validation | fronting group contains at least one routed domain | fronting group domain list is empty | `advanced.fronting_groups.domains` | Advanced -> Fronting groups -> routed domains | Advanced config import -> fronting_groups |
