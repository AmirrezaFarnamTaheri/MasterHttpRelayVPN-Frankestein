# Parity matrix (modes × backends × surfaces)

Source of truth: `docs/parity-matrix.json`.
Generated by `tools/generate-parity-matrix.py`.
Canonical backend IDs: `apps_script, serverless_json, tunnel_node`.

## Modes

| Item | Desktop | Android | Runtime | Docs | CI | Examples | Notes |
|---|---|---|---|---|---|---|---|
| Apps Script (apps_script) | supported | supported | supported | `README.md`, `docs/relay-modes.md`, `docs/advanced-options.md` | `cargo test --features ui`, `apps_script helper tests`, `readiness contract check` | `config.example.json` | Classic local MITM + Apps Script relay; requires account_groups. |
| Direct bootstrap (direct) | supported | supported | supported | `README.md`, `docs/fronting-groups.md`, `docs/relay-modes.md` | `cargo test --features ui`, `docs link+anchor checks` | `config.direct.example.json`, `config.fronting-groups.example.json` | SNI-rewrite only; bootstrap for reaching Apps Script setup pages. |
| Full tunnel (full) | supported | supported | supported | `docs/relay-modes.md`, `docs/udpgw.md`, `tunnel-node/README.md` | `tunnel-node clippy+tests`, `cargo test --features ui` | `config.full.example.json` | End-to-end tunnel via Apps Script + tunnel-node; no local MITM required. |
| Serverless JSON (vercel_edge) | supported | supported | supported | `docs/vercel-json-relay.md`, `docs/netlify-json-relay.md`, `docs/relay-modes.md` | `cargo test --features ui`, `docs link+anchor checks` | `config.vercel-edge.example.json` | Native edge JSON relay; config key remains `vercel` for compatibility. |

## Backends

| Item | Desktop | Android | Runtime | Docs | CI | Examples | Notes |
|---|---|---|---|---|---|---|---|
| Apps Script relay | supported | supported | supported | `assets/apps_script/README.md`, `README.md` | `Apps Script helper tests`, `Apps Script syntax check` | - | Deploy Code.gs/CodeFull.gs variants; compat probe via ?compat=1. |
| Serverless JSON relay (Vercel/Netlify) | supported | supported | supported | `docs/vercel-json-relay.md`, `docs/netlify-json-relay.md` | `JavaScript relay tools syntax`, `cargo tests` | - | Two providers share the same client contract under vercel.* config. |
| tunnel-node | supported | supported | supported | `tunnel-node/README.md`, `docs/udpgw.md` | `cargo clippy (tunnel-node)`, `cargo test (tunnel-node)` | - | Required only for full mode. |
